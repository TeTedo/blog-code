import * as hardhat from "hardhat/config";
import "@nomicfoundation/hardhat-toolbox";

const config: hardhat.HardhatUserConfig = {
  solidity: {
    version: "0.5.16",
  },
};

export default config;
