type SurveyResponse = {
  id: string; // registration id
  name: string;
  hobby: string;
  des: string;
  insta: string;
  personalNumber: string;
  profileImg: string; // 프로필 이미지 URL 추가
  mbti: string;
  drink: string;
};
